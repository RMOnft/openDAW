# Path to custom Vale styles
StylesPath = styles # directory containing style definitions

# Report suggestions at or above the warning level
MinAlertLevel = warning # only show warnings and errors

[*.md]
# Apply our OpenDAW style rules to all Markdown files
BasedOnStyles = OpenDAW # reference style package
