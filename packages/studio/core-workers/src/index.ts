/**
 * Entry point for the studio worker bundle.
 *
 * The compiled output of this module is provided to
 * {@link WorkerAgents.install} so that background worker services can be
 * started by the main thread.
 *
 * @packageDocumentation
 */
export {}
