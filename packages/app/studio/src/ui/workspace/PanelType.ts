/**
 * Enumerates the different panel implementations available in the workspace.
 * Values map to components created by the panel factory.
 */
export enum PanelType {
  Dashboard,
  Timeline,
  ContentEditor,
  DevicePanel,
  BrowserPanel,
  Notepad,
  Mixer,
  ModularSystem,
  VUMeter,
  MidiFall,
  ProjectInfo,
  EmptyFlexSpace,
}
