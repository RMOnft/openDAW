/**
 * Vertical bounds of the area a region occupies on the timeline canvas.
 */
export type RegionBound = {
    top: number
    bottom: number
}

/**
 * Colour configuration used when painting a region and its contents.
 */
export type RegionColors = {
    contentColor: string
}